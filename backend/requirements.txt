# Cerberus Pi backend — Python dependencies.
# Ranges (not exact ==) so pip picks wheels that match the running interpreter.
# Debian 13 (trixie) ships Python 3.13; the floors below all have cp313 ARM64
# wheels, avoiding source builds. Works on Python 3.11–3.13.
Django>=5.1.4,<5.2
djangorestframework>=3.15.2,<3.16
django-cors-headers>=4.4.0
django-filter>=24.3
channels>=4.1.0,<5
channels-redis>=4.2.0
daphne>=4.1.2
psycopg2-binary>=2.9.10
redis>=5.0.7
celery>=5.4,<5.6
django-celery-beat>=2.7.0
python-nmap>=0.7.1
scapy>=2.6.0
reportlab>=4.2.5,<5
weasyprint>=62.3
cryptography>=43.0.1
pycryptodome>=3.21.0
python-dotenv>=1.0.1
gunicorn>=22.0.0
requests>=2.32.3

# --- Phase 11: AI anomaly detection (additive) ---
scikit-learn>=1.5.1
numpy>=2.1.0
pandas>=2.2.3
scipy>=1.14.0
joblib>=1.3.0
imbalanced-learn>=0.12.0
# DoS LSTM is OPTIONAL and ARM64-only. It is NOT required for SQLi/XSS/BruteForce
# or statistical DoS detection. Install separately on the Pi if you want the LSTM
# layer (keeps `pip install -r requirements.txt` working on every platform):
#   pip install --extra-index-url https://google-coral.github.io/py-repo/ tflite-runtime
# tflite-runtime>=2.14.0
